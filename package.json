{
  "name": "tempmail",
  "version": "1.0.0",
  "description": "Free Disposable Email Service - Similar to TempMail.com",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "monitor": "node monitor.js",
    "start:optimized": "NODE_OPTIONS=\"--max-old-space-size=400 --expose-gc\" node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "email",
    "disposable",
    "temporary",
    "mail",
    "service"
  ],
  "author": "TempMail",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
